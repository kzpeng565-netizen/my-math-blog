---
topic: AI内容格式整理
---
**user**: 1. 用$和$$包围latex, 而不是\(和\[
2. 去掉所有的标题, 只使用加粗来强调
3. 删除多余的空行, obsidian的行间公式与正文之间不需要额外空一行
4. 把内容修改简洁一些, 但大意不要改变
5. 保留所有的项目符号, 以及1.2.3.的排版

需要整理的内容如下:

{}

**ai**: 我们需要分步将半圆“展开”：

1. **分式线性变换：** 考虑映射 $w_1 = \frac{z+R}{z-R}$。
   - 当 $z = 0$ 时，$w_1 = -1$。
   - 当 $z \in (-R, R)$（实数直径）时，$w_1$ 是负实数。
   - 当 $z = Re^{i\theta}$（半圆弧）时，代入得 $w_1 = \frac{R(e^{i\theta}+1)}{R(e^{i\theta}-1)} = -i \cot(\frac{\theta}{2})$。因为 $\theta \in (0, \pi)$，$\cot(\frac{\theta}{2}) > 0$，所以 $w_1$ 落在负虚轴上。
   - 综上，$w_1$ 将半圆映射到了**第三象限**（实部为负，虚部为负）。

2. **平方展开：** 为了得到上半平面，我们将其平方：
   $$w = f(z) = w_1^2 = \left( \frac{z+R}{z-R} \right)^2$$
   第三象限的幅角范围是 $(-\pi, -\pi/2)$，平方后幅角变为 $(-2\pi, -\pi)$，对应**上半平面 $\mathbb{H}^+$**。

**利用共形不变性写出解** 上半平面 $\mathbb{H}^+$ 内以 $w_0$ 为极点的格林函数为：
$$G_{\mathbb{H}^+}(w, w_0) = \frac{1}{2\pi} \ln \left| \frac{w - \bar{w}_0}{w - w_0} \right|$$
代入 $w = f(z)$ 和 $w_0 = f(z_0)$，得半圆区域的格林函数：
$$G(z, z_0) = \frac{1}{2\pi} \ln \left| \frac{\left( \frac{z+R}{z-R} \right)^2 - \overline{\left( \frac{z_0+R}{z_0-R} \right)^2}}{\left( \frac{z+R}{z-R} \right)^2 - \left( \frac{z_0+R}{z_0-R} \right)^2} \right|$$