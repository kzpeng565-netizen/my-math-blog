>[!Note] 道路提升的拼接性质
>$$
\widetilde{\alpha\cdot\beta}=\tilde{\alpha}\cdot\tilde{\beta}
>$$
1. 复叠空间中道路提升的拼接等式 $\widetilde{\alpha\cdot\beta}=\tilde{\alpha}\cdot\tilde{\beta}$ 在直观上成立，因为复叠空间局部是底空间的同胚，拼接后提升自然等于分别提升再拼接。
2. 为严谨证明，需确保 $\alpha,\beta$ 满足 $\alpha(1)=\beta(0)$，且 $\tilde{\alpha},\tilde{\beta}$ 的起点衔接：$\tilde{\beta}(0)=\tilde{\alpha}(1)$。
3. 计算投影可知 $p\circ(\tilde{\alpha}\cdot\tilde{\beta})=(\alpha\cdot\beta)$，因此 $\tilde{\alpha}\cdot\tilde{\beta}$ 是 $\alpha\cdot\beta$ 从 $\tilde{x}_0$ 出发的一个提升。
4. 道路提升的唯一性引理保证：从同一起点出发的提升是唯一的。
5. 故 $\widetilde{\alpha\cdot\beta}$ 与 $\tilde{\alpha}\cdot\tilde{\beta}$ 作为同一起点的同一条提升道路必然相等。


定理2（连续映射的提升定理）：已知复迭映射 $p:\left(\widetilde{X}, \widetilde{x}_0\right) \rightarrow\left(X, x_0\right)$ 以及一个连续映射 $f:(Y, r) \rightarrow\left(X, x_0\right)$ 。则映射的提升问题：

![[Pasted image 20260526164627.png|400]]

有解 $\widetilde{f}$ 的充要条件是 $f_*\left(\pi_1(Y, r)\right) \subseteq p_*\left(\pi_1\left(\widetilde{X}, x_0\right)\right)$ ．

定理3（复迭映射的分类）：对于空间 $X$ 上的两个复迭映射 $p_i:\left(\widetilde{X}_i, \widetilde{x}_i\right) \rightarrow\left(X, x_0\right), \quad i=$ 1,2 ，令 $H_i=p_{i *} \pi_1\left(\widetilde{X}_i, \widetilde{x}_i\right) \subset \pi_1\left(X, x_0\right), i=1,2$ 。

1）如果 $H_1 \subseteq H_2$ ，则存在复迭映射 $p: \widetilde{X}_1 \rightarrow \widetilde{X}_2$ ，满足 $p_1=p_2 \circ p$ ：
![[Pasted image 20260526164734.png|200]]
2）$p_1 \cong p_2$ 的充要条件是，子群 $H_1$ 和 $H_2$ 在基本群 $\pi_1\left(X, x_0\right)$ 中共轭。



