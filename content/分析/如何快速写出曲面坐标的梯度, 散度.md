
>当我引入球面坐标的时候, 总会忘记怎么样计算它的梯度, 散度, 旋度, 和拉普拉斯算子. 于是我痛定思痛, 结合物理意义来记忆公式, 没想到一目了然

在进行坐标变换的时候, 我们会遇到尺度上的收缩:
例如$e_{1}'=100e_{1},e_{2}'=e_{2},e_{3}'=e_{3}$. 我们把$(e_{1}',e_{2}',e_{3}')$视为一个相对于物理空间虚构的基, 那么向量的坐标分别是$(x_{1},x_{2},x_{3})$与$(x_{1}',x_{2}',x_{3}')$, 这里显然有$x_{1}'=\frac{1}{100}x_{1}$. 而$100=\left| e_{1}' \right|:=h_{1}$​, 就是所谓的**拉梅系数**

我们看一个更通用的例子:
在二维空间中, 定义换元$x=r\cos\theta,\: y=r\sin\theta$​. 
对于任何一个向量$\boldsymbol{r}=(r\cos\theta,r\sin\theta)$, 有
$$
\begin{align}
\frac{\partial \boldsymbol{r}}{\partial r} =(\cos\theta,\sin\theta)=e_{1}' \\
\frac{\partial \boldsymbol{r}}{\partial \theta} =(-r\sin\theta,r\cos\theta)=e_{2}'
\end{align}
$$
为什么恰好是$e_{2}'$呢, 因为对于$\boldsymbol{r}$我们沿着$\theta$坐标方向变化单位长度, 根据坐标定义, 得到的结果是$d\boldsymbol{r}=e_{2}'d\theta$.
所以我们可以计算得到$\left| e_{1}' \right|=1,\:\left| e_{2}' \right|=\sqrt{r^{2}}=r$. 定义基的长度为拉梅系数
$$
h_{1}=\left| e_{1}' \right| =1,\:h_{2}=\left| e_{2}' \right| =r
$$
那么, 我们就可以完成两个坐标的长度转换:
设一个向量在新的坐标下为$(A_{1},A_{2})$, 那么它是$A_{1}e_{1}'+A_{2}e_{2}'$​. 由于我们选取的坐标是正交的, 它的模长就是$\sqrt{A_{1}^{2}h_{1}^{2}+A_{2}^{2}h_{2}^{2}}$​. 我们完成了两个坐标的转换.

#### 曲线坐标系下的长度变化

类似于前面的讨论, 我们在物理空间中的标准正交基为$e_{1},e_{2},e_{3}$, 我们对坐标进行变换, 新坐标为$(q^{1},q^{2},q^{3})$, 每个分量正交. 虚拟坐标下的正交基为$e_{1}',e_{2}',e_{3}'$, 注意它们都是随着空间变动的.
则我们对向量$\boldsymbol{r}$在$e_{i}$方向变动一个小长度, 向量变动为
$$
d\boldsymbol{r}=e_{i}dq^{i}\implies e_{i}=\frac{\partial \boldsymbol{r}}{\partial q^{i}}
$$
两边取模长得到长度关系:
$$
\left| d\boldsymbol{r} \right| =\left| e_{i} \right| dq^{i}
$$
我们定义$h_{i}=\left| e_{i} \right|$, 那么$ds ^{i}=h_{i}dq^{i}$

#### 梯度的变化

在欧式空间中, 我们定义梯度满足
$$
df=\nabla f\cdot r
$$
我们把这个式子限制在某个向量$v$上
$$
D_{v}f=\nabla f\cdot v
$$
设$\nabla f=z_{1}e_{1}'+z_{2}e_{2}'+z_{3}e_{3}'$​, 我们计算沿着$e_{1}'$的方向导数
$$
D_{e_{1}'}f=\nabla f\cdot e_{1}'
$$
左侧$D_{e_{1}'}f= \frac{\partial f}{\partial q^{1}}$, 二者是同义词, 只是不同说法.
右侧是一个向量与基做内积, 基下的坐标还要除以基的模长 $(\nabla f)_{1}=\frac{\nabla f\cdot e_{1}'}{\left| e_{1}' \right|}=\frac{1}{h_{1}}\frac{\partial f}{\partial q^{1}}$ 
所以我们就得到了
$$
\nabla f=(\frac{1}{h_{i}}\frac{\partial f}{\partial q^{i}} )_{(i=1,2,3)}
$$

#### 散度的变化

为了推导新坐标下的散度, 我们要回归散度的几何定义, 暂时忘记代数定义.
$$
\nabla \cdot \mathbf{A}=\lim _{V \rightarrow 0} \frac{1}{V} \oint_{\partial V} \mathbf{A} \cdot d \mathbf{S} .
$$
右侧是一个与坐标无关的量, 因为$V$显然是几何的, $A\cdot B$也是几何的, 例如$2e_{1}'\cdot3e_{1}'=6h_{1}$. 
我们把这个定义放在新坐标$(q^{1},q^{2},q^{3})$下, 计算它们的几何量, 如图所示
![[Pasted image 20260509232609.png]]
则在这个小方块内对三个方向流量求和, 再除以总体积$V=h_{1}h_{2}h_{3}dq^{1}dq^{2}dq^{3}$, 就得到
$$
\nabla \cdot \mathbf{A}=\frac{1}{h_1 h_2 h_3} \sum_i \frac{\partial}{\partial q^i}\left(\frac{h_1 h_2 h_3}{h_i} A^i\right)
$$

#### 散度的变化

$$
(\nabla \times \mathbf{A}) \cdot \hat{\mathbf{n}}=\lim _{\Delta S \rightarrow 0} \frac{1}{\Delta S} \oint_{\partial S} \mathbf{A} \cdot d \mathbf{l}
$$
散度在某个方向上的分量, 等于垂直于这个分量平面的环流积分的极限. 完全类似于散度, 我们分析两组对边的环流, 可以得到$\left[\frac{\partial\left(A^3 h_3\right)}{\partial q^2}-\frac{\partial\left(A^2 h_2\right)}{\partial q^3}\right] d q^2 d q^3$ 
再除以总的面积
$(\nabla \times \mathbf{A})_1=\frac{1}{h_2 h_3}\left[\frac{\partial\left(A^3 h_3\right)}{\partial q^2}-\frac{\partial\left(A^2 h_2\right)}{\partial q^3}\right]$

#### 拉普拉斯算子

