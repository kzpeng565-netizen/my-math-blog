# 1. 格林函数的对称性

> [!Note] 题目1
> 证明 $G(x,y)=G(y,x)$ （这里我们追求严格的数学证明，因此我们用 $G(x,y)=\Phi(x-y)=\phi^{x}(y)$ 这个定义作为出发点）

对于给定的 $\Omega$ 内两点 $x \neq y$，固定 $x, y$，记：
$$u(z) = G(x,z), \quad v(z) = G(y,z)$$
由格林函数定义，$u$ 在 $\Omega \setminus \{x\}$ 内调和，$v$ 在 $\Omega \setminus \{y\}$ 内调和，且在 $\partial\Omega$ 上 $u=v=0$。
取足够小的 $\varepsilon > 0$，使得闭球 $\bar{B}_\varepsilon(x)$ 与 $\bar{B}_\varepsilon(y)$ 不相交且均包含于 $\Omega$ 内。记截断区域为：
$$V_\varepsilon = \Omega \setminus (\bar{B}_\varepsilon(x) \cup \bar{B}_\varepsilon(y))$$
在 $V_\varepsilon$ 内，$\Delta u = \Delta v = 0$。对 $u, v$ 在 $V_\varepsilon$ 上应用格林第二公式：
$$\int_{V_\varepsilon} (u\Delta v - v\Delta u) dz = \int_{\partial V_\varepsilon} \left( u \frac{\partial v}{\partial \nu} - v \frac{\partial u}{\partial \nu} \right) dS_z = 0$$
其中 $\nu$ 为 $V_\varepsilon$ 的单位外法向量。
由于 $\partial V_\varepsilon = \partial\Omega \cup \partial B_\varepsilon(x) \cup \partial B_\varepsilon(y)$，且在 $\partial\Omega$ 上 $u=v=0$，外边界积分消失，故：
$$\int_{\partial B_\varepsilon(x)} \left( u \frac{\partial v}{\partial \nu} - v \frac{\partial u}{\partial \nu} \right) dS_z + \int_{\partial B_\varepsilon(y)} \left( u \frac{\partial v}{\partial \nu} - v \frac{\partial u}{\partial \nu} \right) dS_z = 0$$
令 $\varepsilon \to 0$。对于 $\partial B_\varepsilon(x)$ 上的积分，注意此时 $\nu$ 指向球心 $x$。$v(z)$ 在 $x$ 附近光滑，而 $u(z) = \Phi(z-x) - \phi^x(z)$。由基本解的奇点提取性质：
$$\lim_{\varepsilon \to 0} \int_{\partial B_\varepsilon(x)} \left( u \frac{\partial v}{\partial \nu} - v \frac{\partial u}{\partial \nu} \right) dS_z = v(x) = G(y,x)$$
同理，对于 $\partial B_\varepsilon(y)$ 上的积分，$u(z)$ 在 $y$ 附近光滑，奇点在 $v(z)$ 上。交换 $u, v$ 的位置提取负号：
$$\lim_{\varepsilon \to 0} \int_{\partial B_\varepsilon(y)} \left( u \frac{\partial v}{\partial \nu} - v \frac{\partial u}{\partial \nu} \right) dS_z = - \lim_{\varepsilon \to 0} \int_{\partial B_\varepsilon(y)} \left( v \frac{\partial u}{\partial \nu} - u \frac{\partial v}{\partial \nu} \right) dS_z = -u(y) = -G(x,y)$$
将上述两项极限代回原等式，得到：
$$G(y,x) - G(x,y) = 0$$
即 $G(x,y) = G(y,x)$
# 2. 上半平面的 Poisson 公式与傅立叶变换

> [!Note] 题目2
> 利用傅立叶方法推导上半平面的 Poisson 公式（特别的，计算 $\frac{1}{1+x^{2}}$ 的傅立叶变换）

上半平面 Dirichlet 问题
$$
\left\{\begin{array}{l}
u_{x x}+u_{y y}=0, \quad y>0, \\
u(x, 0)=g(x) .
\end{array}\right.
$$
将 $y$ 视为时间 $t>0$ ，则方程写作 $u_{t t}+u_{x x}=0$ 。
傅里叶变换得 $\hat{u}_{t t}-\xi^2 \hat{u}=0$ ．
特征根 $\pm|\xi|$ ，通解 $\hat{u}=a(\xi) e^{-|\xi| t}+b(\xi) e^{|\xi| t}$ 。由 $t \rightarrow \infty$ 时有界性舍去发散项，取 $b=0$ ；利用 $\hat{u}(\xi, 0)=\hat{g}(\xi)$ 得 $a=\hat{g}$ ，
$$
\hat{u}(\xi, t)=\hat{g}(\xi) e^{-|\xi| t} .
$$
逆变换与卷积：逆变换得
$$
u(x, t)=\frac{1}{\sqrt{2 \pi}} \int_{-\infty}^{\infty} \hat{g}(\xi) e^{-|\xi| t} e^{i \xi x} d \xi
$$
代入 $\hat{g}(\xi)=\frac{1}{\sqrt{2 \pi}} \int_{-\infty}^{\infty} g(z) e^{-i \xi z} d z$ 并交换积分次序：
$$
u(x, t)=\int_{-\infty}^{\infty} g(z)\left[\frac{1}{2 \pi} \int_{-\infty}^{\infty} e^{-|\xi| t} e^{i \xi(x-z)} d \xi\right] d z
$$
计算泊松核：
$$
\int_{-\infty}^{\infty} e^{-|\xi| t} e^{i \xi(x-z)} d \xi=\int_0^{\infty} e^{-\xi(t-i(x-z))} d \xi+\int_{-\infty}^0 e^{\xi(t+i(x-z))} d \xi=\frac{1}{t-i(x-z)}+\frac{1}{t+i(x-z)}=\frac{2 t}{t^2+(x-z)^2}
$$
因此核函数为 $\frac{t}{\pi\left(t^2+(x-z)^2\right)}$ 。代回 $t=y$ ，即得上半平面的泊松积分公式：
$$
u(x, y)=\frac{y}{\pi} \int_{-\infty}^{\infty} \frac{g(z)}{(x-z)^2+y^2} d z
$$

# 3. 上半平面 Dirichlet 问题的边界连续性

> [!Note] 题目3
> 考虑上半平面的边值问题（假设边界值 $g$ 连续，假设 $u$ 在上半片面内部调和），证明（格林公式）给出的 $u$ 在边界连续，（即 $x$ 趋于边界上 $x^{*}$， 有 $u(x)$ 趋于 $g(x^{*})$）

根据给定条件，设 $u(x,y)=\int_{-\infty}^{+\infty}K_y(x-z)g(z)\,dz$，$K_y(t)=\frac{y}{\pi(t^2+y^2)}$，$g$ 有界且连续于 $x_0$。对 $\forall\varepsilon>0$，由连续性 $\exists\delta>0$ 使 $|z-x_0|<\delta\Rightarrow|g(z)-g(x_0)|<\frac\varepsilon2$。固定 $|x-x_0|<\frac\delta2$，则

$$
|u(x,y)-g(x_0)|\le \underbrace{\int_{|z-x_0|<\delta}K_y(x-z)|g(z)-g(x_0)|dz}_{I_1}+\underbrace{\int_{|z-x_0|\ge\delta}K_y(x-z)|g(z)-g(x_0)|dz}_{I_2}.
$$

由 Poisson 核归一性 $\int K_y=1$，得 $I_1\le\frac\varepsilon2\int_{|z-x_0|<\delta}K_y\le\frac\varepsilon2$。

对 $I_2$，当 $|z-x_0|\ge\delta$ 且 $|x-x_0|<\frac\delta2$ 时 $|x-z|\ge\frac\delta2$，且 $|g(z)-g(x_0)|\le 2M$，故

$$
I_2\le 2M\int_{|t|\ge\frac\delta2}\frac{y}{\pi(t^2+y^2)}\,dt=\frac{4M}{\pi}\Bigl(\frac\pi2-\arctan\frac\delta{2y}\Bigr).
$$

因为 $\arctan\frac\delta{2y}\to\frac\pi2\;(y\to0^+)$，$\exists\eta>0$ 使当 $0<y<\eta$ 时 $I_2<\frac\varepsilon2$。于是当 $|x-x_0|<\frac\delta2$、$0<y<\eta$ 时 $|u(x,y)-g(x_0)|<\varepsilon$，即 $\displaystyle\lim_{(x,y)\to(x_0,0)}u(x,y)=g(x_0)$。


# 4. 圆盘上的 Poisson 公式与边界连续性

> [!Note] 题目4
> 对圆盘平行的完成问题2，3

极坐标下拉普拉斯方程 $u_{rr}+\frac{1}{r}u_r+\frac{1}{r^2}u_{\theta\theta}=0$，设 $u(r,\theta)=R(r)\Theta(\theta)$，得到 $\Theta''+n^2\Theta=0$，$r^2R''+rR'-n^2R=0$。

解得 $\Theta(\theta)=e^{in\theta}$，径向方程通解 $R_n(r)=A_n r^{|n|}+B_n r^{-|n|}$。由 $r=0$ 处有界得 $B_n=0$，故
$$u(r,\theta)=\sum_{n=-\infty}^{\infty}C_n r^{|n|}e^{in\theta}.$$

代入边界条件 $u(1,\theta)=g(\theta)$，得 $g(\theta)=\sum C_n e^{in\theta}$，因此傅里叶系数
$$C_n=\frac{1}{2\pi}\int_0^{2\pi}g(\tau)e^{-in\tau}d\tau.$$
将其代回原式并交换求和与积分：
$$u(r,\theta)=\frac{1}{2\pi}\int_0^{2\pi}g(\tau)\left(\sum_{n=-\infty}^{\infty}r^{|n|}e^{in(\theta-\tau)}\right)d\tau.$$
令 $\varphi=\theta-\tau$，计算内和：
$$
\begin{aligned}
S&=\sum_{n=-\infty}^{\infty}r^{|n|}e^{in\varphi}
=1+\sum_{n=1}^{\infty}(re^{i\varphi})^n+\sum_{n=1}^{\infty}(re^{-i\varphi})^n\\
&=1+\frac{re^{i\varphi}}{1-re^{i\varphi}}+\frac{re^{-i\varphi}}{1-re^{-i\varphi}}
=\frac{1-r^2}{1-2r\cos\varphi+r^2}.
\end{aligned}
$$
圆盘的泊松积分公式：
$$u(r,\theta)=\frac{1}{2\pi}\int_0^{2\pi}\frac{1-r^2}{1-2r\cos(\theta-\tau)+r^2}\,g(\tau)\,d\tau.$$
[[9. 积分核&恒等逼近]]
设
$$
u(r,\theta)=\int_{0}^{2\pi}K(r,\theta-\tau)g(\tau)\,d\tau.
$$
假设核满足
$$
K(r,\theta)\geq 0,\qquad \int_{-\pi}^{\pi}K(r,\theta)\,d\theta=1,
$$
且对任意 $\varepsilon>0$ 有
$$
\lim_{r\to 1^-}\int_{[-\pi,\pi]\setminus U_{\varepsilon}(0)}K(r,\theta)\,d\theta=0.
$$
这说明当 $r\to 1^-$ 时 $K(r,\theta)$ 的质量集中到 $\theta=0$ 附近。

证明由泊松积分得到的 $u(r,\theta)$ 在边界连续：若 $(r,\theta)\to(1,\theta_0)$，则
$$
u(r,\theta)\to g(\theta_0).
$$
由 $\int K(r,\theta)\,d\theta=1$ 得
$$
u(r,\theta)-g(\theta_0)=\int_{0}^{2\pi}K(r,\theta-\tau)\bigl(g(\tau)-g(\theta_0)\bigr)\,d\tau.
$$
记圆周距离
$$
d(\alpha,\beta)=\min_{k\in\mathbb Z}|\alpha-\beta+2k\pi|.
$$
$g$ 在 $\theta_0$ 连续，故对任意 $\varepsilon>0$ 存在 $\delta_1>0$，当 $d(\tau,\theta_0)<\delta_1$ 时
$$
|g(\tau)-g(\theta_0)|<\varepsilon.
$$
将积分分为：
$$
\int_{0}^{2\pi}=\int_{d(\tau,\theta_0)<\delta_1}+\int_{d(\tau,\theta_0)\geq \delta_1}.
$$
由 $K\geq0$ 及 $\int K=1$ 得
$$
\begin{aligned}
&\left|\int_{d(\tau,\theta_0)<\delta_1}K(r,\theta-\tau)\bigl(g(\tau)-g(\theta_0)\bigr)\,d\tau\right|\\
&\leq \varepsilon \int_{d(\tau,\theta_0)<\delta_1}K(r,\theta-\tau)\,d\tau\\
&\leq \varepsilon\int_{0}^{2\pi}K(r,\theta-\tau)\,d\tau=\varepsilon.
\end{aligned}
$$
当 $d(\theta,\theta_0)<\frac{\delta_1}{2}$ 且 $d(\tau,\theta_0)\geq\delta_1$ 时，由三角不等式
$$
d(\theta,\tau)\geq d(\tau,\theta_0)-d(\theta,\theta_0)\geq \frac{\delta_1}{2}.
$$
故远离 $\theta_0$ 的点仍远离核中心 $\theta$。于是
$$
\begin{aligned}
&\left|\int_{d(\tau,\theta_0)\geq\delta_1}K(r,\theta-\tau)\bigl(g(\tau)-g(\theta_0)\bigr)\,d\tau\right|\\
&\leq 2M\int_{d(\tau,\theta_0)\geq\delta_1}K(r,\theta-\tau)\,d\tau\\
&\leq 2M\int_{d(\varphi,0)\geq \delta_1/2}K(r,\varphi)\,d\varphi.
\end{aligned}
$$
由好核的集中性，存在 $\delta_2>0$，当 $r>1-\delta_2$ 时
$$
\int_{d(\varphi,0)\geq \delta_1/2}K(r,\varphi)\,d\varphi<\frac{\varepsilon}{2M},
$$
故远处部分小于 $\varepsilon$。

综上，当 $d(\theta,\theta_0)<\frac{\delta_1}{2}$ 且 $r>1-\delta_2$ 时
$$
|u(r,\theta)-g(\theta_0)|\leq 2\varepsilon,
$$
因此
$$
\lim_{(r,\theta)\to(1,\theta_0)}u(r,\theta)=g(\theta_0).
$$

# 5. 格林函数的性质与半圆区域格林函数

> [!Note] 题目5-1
> 证明格林函数的性质 3 及性质 5 ．
> 性质 3 在区域 $\Omega$ 中成立着不等式：
> $$
> 0<G\left(M, M_0\right)<\frac{1}{4 \pi r_{M_0 M}}
> $$
> 性质 $5$ $\displaystyle\iint_{\Gamma} \frac{\partial G\left(M, M_0\right)}{\partial \boldsymbol{n}} \mathrm{d} S_M=-1$ ．

$\phi^{x}(y)$满足以下方程:
$$
\begin{cases}
\Delta \phi^{x}(y)=0 & \forall y\in\Omega,x\in\Omega \\
\phi^{x}(y)=\Phi(y-x) & \forall y\in \partial\Omega,x\in\Omega 
\end{cases}
$$
显然根据极小值原理 存在$m=\inf_{y\in \partial\Omega}\phi^{x}(y)=\inf_{y\in\Omega}\phi^{x}(y)>0$ , 所以$\phi^{x}(y)>0$ 
$$
G(x,y)=\Phi(y-x)-\phi^{x}(y)<\Phi(y-x)
$$
​$y\to x$时$\Phi \to+\infty$, 因此可以选取趋于$\Omega-B_{\varepsilon}(x)$使得边界上$G(x,y)\geq0$, 而且在$\Omega-B_{\varepsilon}(x)$没有奇点, 所以$G(x,y)\geq0$成立.

性质5
$$
\iint_{\Gamma}-\iint_{\partial B_{\varepsilon}}=\iiint_{\Omega}\Delta_{y} G(x,y)dV_{y}=0
$$
而
$$
\iint_{\partial B_{\varepsilon}(x)}\Phi(y-x)\to1
$$
根据基本解的定义.
由于没有奇性,  $\iint_{\partial B_{\varepsilon}(x)}\phi^{x}(y)=0$.
所以
$$
\iint_{\Gamma}\nabla G(x,y)d\boldsymbol{S}=1
$$

> [!Note] 题目5-2
> 求半圆区域上狄利克雷问题的格林函数．


>[!Note] 格林函数的共形不变性
>设 $\Omega_z$ 与 $\Omega_w$ 为 $\mathbb{C}$ 中的单连通区域，$w = f(z)$ 为共形映射（解析且 $f'(z) \neq 0$），$f$ 将 $\Omega_z$ 映到 $\Omega_w$，将 $\partial\Omega_z$ 映到 $\partial\Omega_w$。则两区域的格林函数满足：
>$$
>G_{\Omega_z}(z, z_0) = G_{\Omega_w}\bigl(f(z), f(z_0)\bigr).
>$$

**证明**：构造 $g(z) = G_{\Omega_w}(f(z), f(z_0))$，验证它满足 $\Omega_z$ 中格林函数的三个性质。

1. **边界为零**：当 $z \to \partial\Omega_z$ 时，$f(z) \to \partial\Omega_w$，而 $G_{\Omega_w}$ 在 $\partial\Omega_w$ 上为 $0$， $g(z) \to 0$。
2. **内部调和**：$G_{\Omega_w}$ 在 $\Omega_w\setminus\{f(z_0)\}$ 内调和，$f(z)$ 解析，复合函数保持调和性（$\Delta_z g = |f'(z)|^2 \Delta_w G = 0$），因此 $g(z)$ 在 $\Omega_z\setminus\{z_0\}$ 内调和。
3. **奇点**：在 $w_0 = f(z_0)$ 附近，$G_{\Omega_w}$ 具有展开
$$
G_{\Omega_w}(w, w_0) = -\frac{1}{2\pi}\ln|w - w_0| + \text{调和部分}
$$
将 $w = f(z)$ 代入，由 $f(z) - f(z_0) \approx f'(z_0)(z - z_0)$ 得
$$
-\frac{1}{2\pi}\ln|f(z)-f(z_0)| = -\frac{1}{2\pi}\ln|z-z_0| - \frac{1}{2\pi}\ln|f'(z_0)|.
$$
其中 $-\frac{1}{2\pi}\ln|f'(z_0)|$ 为常数，可并入调和修正部分。故 $g(z)$ 在 $z_0$ 处具有正确的对数奇点 $-\frac{1}{2\pi}\ln|z-z_0|$

**题目解答:** 
取保角变换  
$$w = f(z) = \left( \frac{z+R}{z-R} \right)^2$$  
它将上半圆盘 $\{ |z|<R,\ \operatorname{Im}z>0 \}$ 映射到上半平面 $\mathbb{H}^+$。  

记 $w_0 = f(z_0)$，上半平面的格林函数为  
$$
G_{\mathbb{H}^+}(w,w_0) = \frac{1}{2\pi}\ln\left|\frac{w-\overline{w_0}}{w-w_0}\right|
$$  由共形不变性得半圆区域的格林函数  
$$G(z,z_0) = \frac{1}{2\pi}\ln\left| 
\frac{\left(\frac{z+R}{z-R}\right)^2 - \overline{\left(\frac{z_0+R}{z_0-R}\right)^2}}{\left(\frac{z+R}{z-R}\right)^2 - \left(\frac{z_0+R}{z_0-R}\right)^2}
\right|$$


# 6. 球内 Dirichlet 问题的 Poisson 公式应用

> [!Note] 题目6
> 利用泊松公式求边值问题
> $$
> \left\{\begin{array}{l}
> u_{x x}+u_{y y}+u_{z z}=0, \quad x^2+y^2+z^2<1, \\
> \left.u(r, \theta, \varphi)\right|_{r=1}=3 \cos 2 \theta+1 \quad(r, \theta, \varphi \text { 表示球面坐标 })
> \end{array}\right.
> $$

# 7. 圆盘上泊松方程 Dirichlet 问题

> [!Note] 题目7
> 求泊松方程狄利克雷问题
> $$
> \begin{cases}\Delta u=x^2 y, & x^2+y^2<a^2 \\ u=0, & x^2+y^2=a^2\end{cases}
> $$
> 的解．

# 8. 平行线间的格林函数

> [!Note] 题目8
> 求 $\mathbf{R}^2$ 中调和方程在两平行线间的格林函数．

引入指数映射：

$$w = f(z) = e^{\frac{\pi}{a}z}$$

- 当 $y = 0$（下边界）时，$w = e^{\frac{\pi}{a}x} > 0$，映射为正实轴。
- 当 $y = a$（上边界）时，$w = e^{\frac{\pi}{a}(x+ia)} = -e^{\frac{\pi}{a}x} < 0$，映射为负实轴。
- 内部区域 $0 < y < a$ 被精确映射为**上半平面** $\mathbb{H}^+ = \{ w \in \mathbb{C} \mid \text{Im}(w) > 0 \}$。

源点 $z_0$ 被映射到上半平面内的点 $w_0 = e^{\frac{\pi}{a}z_0}$。

**利用共形不变性求解**：已知上半平面 $\mathbb{H}^+$ 的标准格林函数为 
$$
G_{\mathbb{H}^+}(w, w_0) = \frac{1}{2\pi} \ln \left| \frac{w - \bar{w}_0}{w - w_0} \right|
$$
由于二维拉普拉斯算子在解析变换下具有共形不变性，直接将映射关系代回，即得到平行线带状区域的格林函数：
$$G(z, z_0) = \frac{1}{2\pi} \ln \left| \frac{e^{\frac{\pi}{a}z} - e^{\frac{\pi}{a}\bar{z}_0}}{e^{\frac{\pi}{a}z} - e^{\frac{\pi}{a}z_0}} \right|$$
