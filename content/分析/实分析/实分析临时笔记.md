(ii) Egorov定理的$\varepsilon$不能取0
$f_{n}(x)=x^{n}$. 在$(0,1)$上处处收敛到0, 但不一致收敛到0.
若只去掉一个零测集$H$, 则$\{f_{n}\}$在$(0,1)\backslash H$上一定不会一致收敛到0.
$\forall n,\:(0,1)\backslash H$中一定存在$\{x_{m}\}$收敛于1, 且$m$充分大的时候
$$
\left| f_{n}(x_{m})-0 \right|=x_{m}^{n}\geq \frac{1}{2} 
$$
(证明不是一致收敛: 取点列$x_{n}$, 使得$\left| f_{n}(x_{n})-f(x_{n}) \right|>\varepsilon$)

**近一致收敛**
设$\{f_{k}\}$, $f$在可测集$E\subset \mathbb{R}^{n}$上可测. 若$\varepsilon>0$, 存在可测集$A,\: m(A)<\varepsilon$, 且$f_{k}$在$E\backslash A$上一致收敛到$f$, 称$f_{k}\overset{a.u.}{\to}f$ (almost uniformly)

命题: Egorov逆定理, 可测集$E$上$\{f_{n}\}\overset{a.u.}{\to}f\implies \{f_{n}\}\overset{a.e.}{\to}f$
$\forall k\in \mathbb{N}$, 存在可测集$E_{k}\subset E$, 使得$m(E-E_{k})\leq \frac{1}{k}$, 且$f_{n}\rightrightarrows f$在$E_{k}$上
$E^{*}=\bigcup_{k=1}^{\infty}E_{k}$, 故$f_{n}\to f$在$E^{*}$上
$0\leq m(E-E^{*})\leq m(E-E_{k})\leq \frac{1}{k}\to0$

**依测度收敛**
设$\{f_{n}(x)\}$是可测集$E$上几乎处处有限的可测函数, 若$\forall\varepsilon>0$
$$
\lim_{k\to\infty} m(\{x\in E\mid \left| f_{k}(x)-f(x) \right| \geq\varepsilon\})=0
$$
则称$\{f_{n}(x)\}$依测度收敛于$f(x)$, 记为$f_{k}\overset{m}{\to}f$​	

命题: 若$\{f_{k}(x)\}$在$E$上同时依测度收敛于$f(x),g(x)$, 则$f=g$ a.e
证明:
$\varepsilon\leq \left| f(x)-g(x) \right|\leq \left| f(x)-f_{k}(x) \right|+\left| f_{k}(x)-g(x) \right|$
$$
\{\left| f-g \right| \geq\varepsilon\}\subset \left\{ \left| f-f_{k} \right| \geq\frac{\varepsilon}{2} \right\}\cup \left\{ \left| f_{k}-g \right| \geq  \frac{\varepsilon}{2} \right\}
$$
$$
0\leq m(\{\left| f-g \right| \geq\varepsilon\})\leq m\left( \left\{ \left| f-f_{k} \right| \geq \frac{\varepsilon}{2} \right\} \right)+m\left( \left\{ \left| f_{k}-g \right| \geq \frac{\varepsilon}{2} \right\} \right)\to0
$$

再令$\varepsilon= \frac{1}{n}$, 可列个零测集的并也是零测集

lebesgue定理
$\{f_{k}\}$是可测集$E$上几乎处处有限的可测函数列, $m(E)<\infty$. 若$\{f_{k}(x)\}$几乎处处收敛于几乎处处有限的函数$f(x)$, 则在$E$上, $f_{k}\overset{m}{\to}f$.

证明:
$$
E_{k}(\varepsilon)=\{x\in E\mid \left| f_{k}(x)-f(x) \right| \geq\varepsilon\}
$$
$$
m\left( \bigcup_{i=1}^{\infty}E_{k}(\varepsilon) \right)
$$

命题 1．18．设 $f(x), f_1(x), \cdots, f_k(x), \cdots$ 是可测集 $E$ 上几乎处处有限的可测函数。在 $E$上，若 $f_k \stackrel{\text { a．u．}}{\rightrightarrows} f$ ，则 $f_k \xrightarrow{m} f$ 。
$f_k \stackrel{\text { a．u．}}{\rightrightarrows} f$ $\implies$ $\forall\varepsilon,\delta>0$, 存在$E_{\delta}\subset E$, 且$m(E_{\delta})<\delta$, $k_{0}\in \mathbb{N}$, s.t. 当$k\geq k_{0}$时
$$
\left| f_{k}(x) -f(x)\right|<\varepsilon \quad x\in E-E_{\delta}
$$
当$k\geq k_{0}$时

$$
m(\{x\in E\mid \left| f_{k}(x)-f(x) \right| \geq\varepsilon\})\leq m(E_{\delta})<\delta\implies f_{k}\overset{m}{\to}f
$$

​	定义1．8．设 $\left\{f_k(x)\right\}$ 是可测集 $E$ 上几乎处处有限的可测函数列。若 $\forall \varepsilon>0$ ，有
$$
\lim _{k, j \rightarrow \infty} m\left(\left\{x \in E:\left|f_k(x)-f_j(x)\right| \geqslant \varepsilon\right\}\right)=0,
$$
则称 $\left\{f_k(x)\right\}$ 为 $E$ 上的依测度 Cauchy 列。

命题 1．19．若 $\left\{f_k(x)\right\}$ 是可测集 $E$ 上的依测度 Cauchy 列，则在 $E$ 上存在几乎处处有限的可测函数 $f(x)$ ，使得在 $E$ 上，$f_k \xrightarrow{m} f$ 。
Part1.
$\forall i\in \mathbb{N},\exists k_{i},s.t.$当$l,j\geq k_{i}$时, 
$$
m(\{x\in E\mid\left| f_{l}(x)-f_{j}(x) \right| \geq 2^{-i}\})<2^{-i}
$$
不妨$k_{1}<k_{2}<\dots$
$$
E_{i}=\{x\in E\mid \left| f_{k_{i+1}}(x)-f_{k_{i}}(x) \right| \geq2^{-i}\}\implies m(E_{i})<2^{-i},\:\forall i\in \mathbb{N}
$$
$S=\bigcap_{j=1}^{\infty}\bigcup_{i=j}^{\infty}E_{i}$
$$
m\left( \bigcup_{i=j}^{\infty}E_{i} \right)\leq \sum_{i=j}^{\infty} m(E_{i})<\sum_{i=j}^{\infty} 2^{-i}=2^{-j+1}
$$
$$
0\leq m(S)\leq2^{-j+1},\:\forall j\in \mathbb{N}\implies m(S)=0
$$
$x\not\in S$ 推出 $x\in \bigcup_{j=1}^{\infty}\left( \bigcup_{i=j}^{\infty}E_{i} \right)^{c}\implies \exists j\in \mathbb{N},s.t.\:x\in E-\bigcup_{i=j}^{\infty}E_{i}$.

Part2
证明$\{f_{k_{i}}\}$ 在 $E-\bigcup_{i=j}^{\infty}E_{i}$上一致收敛, 且极限几乎处处有限
$$
\forall x\in E-\bigcup_{i=j}^{\infty}E_{i}\:, \text{when }i\geq j,\:\left| f_{k_{i+1}}(x)-f_{k_{i}}(x) \right|<2^{-i}
$$
$$
\overset{\forall l\geq j}{\implies}\sum_{i=l}^{\infty} \left| f_{k_{i+1}}(x)-f_{k_{i}}(x) \right| <2^{-l+1}
$$
$$
f(x):=f_{k_{m}}(x)+\sum_{i=m}^{\infty} [f_{k_{i+1}}(x)-f_{k_{i}}(x)]\implies\text{收敛极限几乎处处有限}
$$
$$
\left| f_{k_{m}}(x)-f(x) \right| \leq \sup\sum_{l=m}^{\infty}\left| f_{k_{l+1}}(x)-f_{k_{l}}(x) \right| <\sup2^{-m+1}\to0 \quad m\to \infty
$$

Part 3
$\{f_{k_{i}}\}$依测度收敛
$m\left( \bigcup_{i=j}^{\infty}E_{i} \right)<2^{-i+1}$ 根据命题1.18​$\implies$ $f_{k_{i}}\overset{m}{\to}f$

Part 4
原函数列收敛
$$
0\leq m(\{\left| f_{k}-f \right| \geq\varepsilon\})\leq m\left( \left\{ \left| f_{k}-f_{k_{i}} \right| \geq\frac{\varepsilon}{2} \right\} \right)+m\left( \left\{ \left| f_{k_{i}}-f \right| \geq\frac{\varepsilon}{2} \right\} \right)
$$
$$
\implies f_{k}\overset{m}{\to}f
$$

命题 1.20 （Riesz 定理）．在可测集 $E$ 上，若 $f_k \xrightarrow{m} f$ ，则存在子列 $\left\{f_{k_i}(x)\right\}$ ，使得 $f_{k_i} \xrightarrow{\text { a．e．}} f$ 。

证明:
$f_{k}\overset{m}{\to}f\implies \{f_{k}\}$是依测度Cauchy​	列, 命题1.19$\implies$存在子列$\{f_{k_{i}}\}$收敛于g
$$
\{f_{k_{i}}\}\subset \{f_{k}\}\implies f_{k_{i}}\overset{m}{\to}f
$$
$$
\implies g\overset{a.e.}{=}f
$$

定理 1.21 （Lusin 定理：可测 ⇒ 拟连续，1912）。设 $f$ 为可测集 $E$ 上的 a．e．有限的可测函数。则对任给的 $\varepsilon>0$ ，存在闭集 $F_{\varepsilon} \subset E$ ，使得 $m\left(E \backslash F_{\varepsilon}\right) \leqslant \varepsilon$ 且 $\left.f\right|_{F_{\varepsilon}}$ 连续（也称 $f$ 在 $E$上拟连续）。

不妨设$f$在$E$上处处有限. $\mathbb{Q}=\{r_{n}\}_{n=1}^{\infty}$
$\forall n,k\in \mathbb{N}$, $E_{n,k}:=\left\{ x\in E\mid r_{n}\leq f(x)<r_{n}+ \frac{1}{k} \right\}$ 是可测集
$$
\forall\varepsilon>0, \exists \text{open} \:\mathcal{O}_{n,k}\supset E_{n,k},\:E_{\varepsilon}=A-A_{\varepsilon}
$$
$$
m(E-E_{\varepsilon})\leq m(A_\varepsilon)\leq \sum_{m,k=1}^{\infty} \frac{\varepsilon}{2^{n+k+1}}=\frac{\varepsilon}{2}

$$
因为$E_{\varepsilon}$可测, 
$$
\exists\text{close} \: F_{\varepsilon}\subset E_{\varepsilon}\: s.t.\: m(E_{\varepsilon}-F_{\varepsilon})\leq \frac{\varepsilon}{2}\implies m(E-F_{\varepsilon})\leq m(E-E_\varepsilon)+m(E_{\varepsilon}-F_{\varepsilon})\leq\varepsilon
$$
只需证明$f|_{E_{\varepsilon}}$连续
$\forall x_{0}\in E_{\varepsilon}$, $\forall\sigma>0$​	, 存在$k> \frac{1}{\sigma},k\in \mathbb{N}$, 和$m\in \mathbb{N}$, s.t.
$$
r_{m}\leq f(x_{0})<r_{m}+ \frac{1}{k}\implies x_{0}\in E_{m,k}\subset \mathcal{O}_{m,k}
$$
$$
\implies \exists B(x_{0},r)\subset \mathcal{O}_{m,k}\: s.t.\: \forall x\in B(x_{0},r)\cap E_{m,k}
$$
有$\left| f(x)-f(x_{0}) \right|< \frac{1}{k}<\sigma$
$$
\implies f在x_{0}处连续\implies在E_{\varepsilon}上连续\implies f在F_{\varepsilon}上连续
$$
$$
E_{\varepsilon}=E-A_{\varepsilon}=E-\bigcup_{n,k=1}^{\infty}(\mathcal{O}_{n,k}-E_{n,k})=E\cap \bigcap_{n,k=1}^{\infty}(\mathcal{O}_{n,k}^{c}\cup E_{n,k})\subset E\cap(\mathcal{O}_{m,k}^{c}\cup E_{m,k})
$$
$B(x_{0},r)\subset \mathcal{O}_{m,k}$
$$
B(x_{0},r)\cap E_{\varepsilon}\subset\underbrace{(B(x_{0},r)\cap E\cap \mathcal{O}_{m,k}^{c})}_{\emptyset}\cup(E_{m,k}\cap B_{x_{0},r})
$$

$E=[0,1]$,
$$
f(x)=
\begin{cases}
1 & x\in E\cap \mathbb{Q} \\
0, & x\in E-\mathbb{Q} 
\end{cases}
$$
$f(x)$处处不连续
$E\cap \mathbb{Q}=\{r_{k}\}_{k=1}^{\infty}$ 取$\delta>0$​	充分小, $G=\bigcup_{k=1}^{\infty}\left( r_{k}- \frac{\delta}{2^{k+1}},r_{k}+ \frac{\delta}{2^{k+1}} \right)$ 开
$$
m(G)\leq \sum_{k=1}^{\infty}  \frac{\delta}{2^{k}}=\delta
$$
$G\not\subset E$, $r_{k}=0$, 令$F=E-G$​	非空(只含无理数)
$f|_{F}\equiv0$连续

