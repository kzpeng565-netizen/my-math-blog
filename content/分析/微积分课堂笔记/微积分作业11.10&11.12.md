---
tags:
  - 微积分
  - 分析
---
# 习题课例题

$(u*v)(x)=\int_{\mathbb{R}}u(y)v(x-y)dy$
$suppf\cdot g=suppf\cap suppg$ $supp(f*g)\subset suppf+suppg$ 这里的加法是实数的加法

Young不等式 $1+ \frac{1}{r}=\frac{1}{p}+\frac{1}{q}$
$$
\lVert f*g \rVert _{L^{r}}\leq \lVert f \rVert _{L^{p}}\lVert g \rVert _{L^{q}}
$$
充分条件
1. $p=q=2,r=\infty$
2. $p=1,q=\infty,r=\infty$
3. 紧支集
4. $r=\infty,q=p'$

卷积可以让函数变光滑
$D^{k}(u*v)=u*(D^{k}v)$, 如果$v$可微, $u$不可微, 卷积之后仍然可微

​	[[微积分08 函数卷积#^128014]] $\delta$型函数的三条定义
磨光核
$$
 \varphi(x):= \begin{cases}\exp \left(\frac{1}{|x|^2-1}\right), & |x|<1 \\ 0, & |x| \geqslant 1\end{cases}
 $$
## 例1

1．在 $\mathbb{R}$ 上，$\left\{f_j(x)=\frac{1}{\pi} \cdot \frac{\sin j x}{x}: j \in \mathbb{N}\right\}$ 是局部可积函数列，从而可以看成正则广义函数列．证明：在 $\mathcal{D}^{\prime}$ 中， $\lim _{j \rightarrow \infty} f_j=\delta$ ．

证 $\forall \varphi \in \mathcal{D}$ ，显然存在 $M_0>0$ 使得 $\operatorname{supp} \varphi \subset\left[-M_0, M_0\right]$ ．因为 $\forall j \in \mathbb{N}$ ，

$$
\lim _{M \rightarrow+\infty} \frac{1}{\pi} \int_{-M}^M \frac{\sin j x}{x} \mathrm{~d} x=\lim _{M \rightarrow+\infty} \frac{1}{\pi} \int_{-M}^M \frac{\sin y}{y} \mathrm{~d} y=\frac{1}{\pi} \int_{-\infty}^{+\infty} \frac{\sin y}{y} \mathrm{~d} y=1
$$

所以 $\forall \varepsilon>0$ ，存在 $M_1>M_0$ 使得 $\forall M>M_1$ ，
$$
\left|\frac{1}{\pi} \int_{-M}^M \frac{\sin j x}{x} \mathrm{~d} x-1\right|<\frac{\varepsilon}{2(|\varphi(0)|+1)}
$$

任意固定 $M>M_1$ ，由黎曼引理可知，存在 $N \in \mathbb{N}$ 使得 $\forall j>N$ ，

$$
\frac{1}{\pi}\left|\int_0^M \frac{\varphi(x)+\varphi(-x)-2 \varphi(0)}{x} \sin j x \mathrm{~d} x\right|<\frac{\varepsilon}{2} .
$$
因此当 $j>N$ 时，

$$
\begin{aligned}
& \left|\left\langle f_j, \varphi\right\rangle-\langle\delta, \varphi\rangle\right|=\left|\left\langle f_j, \varphi\right\rangle-\varphi(0)\right| \\
= & \left|\frac{1}{\pi} \int_{-M}^M \frac{\sin j x}{x}(\varphi(x)-\varphi(0)) \mathrm{d} x+\varphi(0)\left(\frac{1}{\pi} \int_{-M}^M \frac{\sin j x}{x} \mathrm{~d} x-1\right)\right| \\
\leqslant & \frac{1}{\pi}\left|\int_0^M \frac{\varphi(x)+\varphi(-x)-2 \varphi(0)}{x} \sin j x \mathrm{~d} x\right|+\frac{|\varphi(0)| \varepsilon}{2(|\varphi(0)|+1)}<\frac{\varepsilon}{2}+\frac{\varepsilon}{2}=\varepsilon .
\end{aligned}
$$
这就证明了 $\lim _{j \rightarrow \infty} f_j=\delta$ ．

黎曼引理:

> [!Note]  Riemann Lebesgue Lemma	
>函数$g$在有限区间$[a,b]$上可积, 则
>$$
\lim_{j\to\infty} \int_{a}^{b} g(x)\sin jx \, dx =0
>$$


# 题目3 数列的卷积

> [!Note] 题目3
> 我们知道，函数 $A(x)=\sum_{n=0}^{\infty} a_n x^n$ 称为数列 $a_0, a_1, \cdots$ 的生成函数．
> 设给定两个数列 $\left\{a_k\right\},\left\{b_k\right\}$ 。如果认为当 $k<0$ 时 $a_k=b_k=0$ ，则自然把数列 $\left\{a_k\right\}$ 与 $\left\{b_k\right\}$的卷积定义为数列 $\left\{c_k=\sum_m a_m b_{k-m}\right\}$ ．请证明：两个数列的卷积的生成函数等于这两个数列的生成函数之积．

计算
$$
\sum_{n=0}^{\infty} b_{n}x^{n}\sum_{m=1}^{\infty} a_{m}x^{m}
$$
对$x^{k}$项计算,对于$x^{m}$,  $\{b_{n}\}$的生成函数需要项$b_{k-m}x^{k-m}$相乘
从而结果为​$\sum_{m=0}^{\infty}\sum_{k=0}^{\infty}a_{m}b_{k-m}x^{k}=\sum_{k=0}^{\infty}\left( \sum_{m=0}^{\infty}a_{m}b_{k-m} \right)x^{k}$

# 题目4

> [!Note] 题目4-a
> a）请验证：如果卷积 $u * v$ 有定义，函数 $u, v$ 中的一个是以 $T$ 为周期的周期函数，则 $u * v$ 也是以 $T$ 为周期的周期函数．

不妨设$v$是周期函数
$$
f(x)=\int u(y)v(x-y)dy\:;\quad f(x+T)=\int u(y)v(x-y+T)dy=f(x)
$$

> [!Note] 题目4-b
> b）请证明用三角多项式逼近连续周期函数的魏尔斯特拉斯定理（见附注 5）．

^fd2a2c
[[微积分08 函数卷积#^f4e109|构造的函数]] $\Delta_{n}=$
$$
\begin{cases}
\frac{\cos ^{2n}x}{\int_{-\frac{\pi}{2}}^{\pi/2} \cos ^{2n}x \, dx } & \lvert x \rvert \leq \frac{\pi}{2} \\
0 & \lvert x \rvert >\frac{\pi}{2}
\end{cases}
$$
是一个 $\delta$​	型函数, 记为$c_{n}\cos ^{2n}x$
$$
f*\Delta_{n}=c_{k}\int_{-\frac{\pi}{2}}^{\pi/2} f(y)\cos ^{2n}(x-y) \, dy
$$
$\cos ^{2}(x-y)= \frac{1+\cos2x}{2}\cos ^{2}y+ \frac{1-\cos2x}{2}\sin ^{2}y+\sin2x\sin y\cos y=a_{1}\cos2x+a_{2}\sin2x=a_{3}\sin(2x+\varphi)$
​利用欧拉定理可以证明$\sin ^{n}\theta=\sum_{k=1}^{n}a_{k}\sin k\theta+b_{k}\cos k\theta$ 
从而$f*\Delta_{n}$是三角多项式

### 题目4习题课补充
$$
\begin{aligned}
& =\int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} f(y) s_n\left(\frac{\mathrm{e}^{\mathrm{i}(x-y)}+\mathrm{e}^{-\mathrm{i}(x-y)}}{2}\right)^{2 n} \mathrm{~d} y \\
& =\int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} f(y) \frac{s_n}{2^{2 n}} \sum_{j=0}^{2 n} \mathrm{C}_{2 n}^j \mathrm{e}^{2(n-j) \mathrm{i}(x-y)} \mathrm{d} y \\
& =\int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} f(y) \frac{s_n}{2^{2 n}}\left(\mathrm{C}_{2 n}^n+2 \sum_{j=0}^{n-1} \mathrm{C}_{2 n}^j \cos (2(n-j)(x-y))\right) \mathrm{d} y \\
& =\int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} f(y) \frac{s_n}{2^{2 n}}\left(\mathrm{C}_{2 n}^n+2 \sum_{k=1}^n \mathrm{C}_{2 n}^{n-k} \cos (2 k(x-y))\right) \mathrm{d} y \\
& =\int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} f(y) \frac{s_n}{2^{2 n}}\left(\mathrm{C}_{2 n}^n+2 \sum_{k=1}^n \mathrm{C}_{2 n}^{n-k}(\cos 2 k x \cos 2 k y+\sin 2 k x \sin 2 k y)\right) \mathrm{d} y
\end{aligned}
$$
直接使用欧拉公式+二项式展开计算

> [!Note] 题目4-c
> c）请证明在附注 4 中指出的加强的魏尔斯特拉斯逼近定理．

> [!Note] 命题5
>设 $f: \mathbb{R} \rightarrow \mathbb{C}$ 是**有界函数**, $\{\Delta_\alpha, \alpha \in A\}$ 当 $\alpha \rightarrow \omega$ 时是 $\delta$ 型函数族。如果对于任何 $\alpha \in A$, 卷积 $f * \Delta_\alpha$ 存在, 并且函数 $f$ 在集合 $E \subset \mathbb{R}$ 上**一致连续**, 则当 $\alpha \rightarrow \omega$ 时, 在 $E$ 上 $(f * \Delta_\alpha)(x) \rightrightarrows f(x)$。

根据命题5, 对于开集的任何紧子集, 函数满足有界性和一致连续性, 命题5成立
%%在闭包上是否无法直接使用命题5?%%
# 题目6

> [!Note] 题目6-a
> a）设
> $$
> \varphi(x):= \begin{cases}a \cdot \exp \left(\frac{1}{|x|^2-1}\right), & |x|<1, \\ 0, & |x| \geqslant 1,\end{cases}
> $$
> 其中 $a$ 是利用条件 $\int_{\mathbb{R}} \varphi(x) d x=1$ 选取的常数．请验证：当 $\alpha \rightarrow+0$ 时，函数族 $\varphi_\alpha(x)=\frac{1}{\alpha} \varphi\left(\frac{x}{\alpha}\right)$是 $\mathbb{R}$ 上的 $C_0^{(\infty)}$ 类 $\delta$ 型函数族．

1. $\varphi_{\alpha}(x)>0$
2. $\int_{\mathbb{R}}\varphi(x)dx=1$
3. 我们计算$\int_{\varepsilon}^{1/\alpha} \varphi_{\alpha}(x) \, dx=\int_{\frac{\varepsilon}{\alpha}}^{1} \varphi\left(t\right) \, dt$. 所以$\lim_{\alpha\to0+}\int_{\frac{\varepsilon}{\alpha}}^{1/\alpha} \varphi(t) \, dt=0$

> [!Note] 题目6-b
> b）对于任何区间 $I \subset \mathbb{R}$ 和任何 $\varepsilon>0$ ，请构造具有以下性质的 $C_0^{(\infty)}$ 类函数 $e(x)$ ：在 $\mathbb{R}$ 上 $0 \leqslant e(x) \leqslant 1 ; e(x)=1 \Leftrightarrow x \in I ; \operatorname{supp} e \subset I_{\varepsilon}$ ，其中 $I_{\varepsilon}$ 是 $\mathbb{R}$ 中的集合 $I$ 的 $\varepsilon$ 邻域（请验证：如果取相应的值 $\alpha>0$ ，则可以取 $\chi_I * \varphi_\alpha$ 作为 $\left.e(x)\right)$ ．

$$
\begin{align}
\int_{a}^{b} \varphi_{\alpha}(x-y) \, dy=  & \int_{a}^{b} \varphi\left( \frac{x-y}{\alpha} \right) \, d\frac{y-x}{\alpha}  \\
= & \int_{\frac{a-x}{\alpha}}^{\frac{b-x}{\alpha}}\varphi(t)  \, dt 
\end{align}
$$
对于$I$内的, 与边界距离大于$\delta$的紧子集, 可以有固定的$\alpha=\delta$满足条件, 但是对极限点包括$a,b$的集合, 结论应该不成立.

> [!Note] 题目6-c
> c）请证明：对于任何 $\varepsilon>0$ ，由函数 $e_k \in C_0^{(\infty)}$ 构成并且具有以下性质的可数的函数族 $\left\{e_k\right\}$ （ $\mathbb{R}$ 上的单位区间的 $\varepsilon$ 分割）存在：$\forall k \in \mathbb{N}, \forall x \in \mathbb{R}\left(0 \leqslant e_k(x) \leqslant 1\right)$ ；该函数族中任何函数的支撑集 $\operatorname{supp} e_k$ 的直径不大于 $\varepsilon>0$ ；任何一个点 $x \in \mathbb{R}$ 只属于有限个集合 $\operatorname{supp} e_k$ ；在 $\mathbb{R}$ 上 $\sum_k e_k(x) \equiv 1$.

对每一个整数区间$[n,n+1]$取这样的函数
取6-b中的$\alpha= \frac{\varepsilon}{2}$, 取分割间距$\delta= \frac{\varepsilon}{2}$, 定义$f_{k}^{n}=\varphi_{\alpha}(x-n-\delta k),0<k\leq[\frac{2}{\varepsilon}]$, 则每一个函数的间隔均为$\delta$, 根据6-b的结论, 任何一点均至少属于一个$f_{k}$的支撑集, 从而不等于0. 根据定义, 紧支撑集的重叠是有限的.
定义$e_{k}= \frac{f_{k}}{\sum_{n}\sum_{k}f_{k}^{n}}$, 分母不为0, 所以是良定义的, 并且是光滑函数.

#### 答案
（3）对任意的 $\varepsilon>0$ ，显然开区间族 $\left\{I_j:=\left(j \frac{\varepsilon}{6},(j+2) \frac{\varepsilon}{6}\right): j \in \mathbb{Z}\right\}$ 构成 $\mathbb{R}$ 的开覆盖。由（2）可知，$\forall j \in \mathbb{Z}$ ，存在函数 $\psi_j(x) \in C_0^{(\infty)}$ 满足：在 $\mathbb{R}$ 上， $0 \leqslant \psi_j(x) \leqslant 1 ; \psi_j(x)=1 \Leftrightarrow x \in I_j ; \operatorname{supp} \psi_j \subset I_{j, \varepsilon / 3}=\left((j-2) \frac{\varepsilon}{6},(j+4) \frac{\varepsilon}{6}\right)$ 。又易见任一 $x \in \mathbb{R}$ 只属于有限个开区间 $I_{j, \varepsilon / 3}$ ．记 $\lambda(x)=\sum_{j \in \mathbb{Z}} \psi_j(x), x \in \mathbb{R}$ ．显然 $\forall x \in \mathbb{R}$ ， $1 \leqslant \lambda(x)<+\infty$ 且 $\lambda(x) \in C_0^{(\infty)}$ ．对 $k \in \mathbb{N}$ ，令 $\alpha(k)=\frac{(-1)^k-1}{4}-\frac{(-1)^k}{2} k$ ， $e_k(x)=\frac{\psi_{\alpha(k)}(x)}{\lambda(x)}$ ，则 $\left\{e_k\right\}_{k \in \mathbb{N}}$ 即满足要求．==最后一步是把$\mathbb{Z}$的索引改到$\mathbb{N}$==


> [!Note] 题目6-d
> d）请证明：对于开集 $G \subset \mathbb{R}$ 上的任何开覆盖 $\left\{U_\gamma, \gamma \in \Gamma\right\}$ 和任何函数 $\varphi \in C^{(\infty)}(G)$ ，由函数 $\varphi_k \in C_0^{(\infty)}$ 构成并且具有以下性质的序列 $\left\{\varphi_k, k \in \mathbb{N}\right\}$ 存在：$\forall k \in \mathbb{N}, \exists \gamma \in \Gamma\left(\operatorname{supp} \varphi_k \subset U_\gamma\right)$ ；任何一个点 $x \in G$ 只属于有限个集合 $\operatorname{supp} \varphi_k$ ；在 $G$ 上 $\sum_k \varphi_k(x)=\varphi(x)$ ．

根据开集的基本定理, 每一个$U_{\gamma}$可以表示为可数个开区间的并集, 我们可以分割加细这些开区间, 使得这些开区间每个点​	至多只有可数的重叠. 同样地, 对所有$U_{\gamma}$产生的开区间族$A=\{I_{\gamma}^{n}\}$加细, 使它每个点至多可数个重叠, 记这些开区间为$U_{k}$ 
对每一个开区间构造6-c中的单位分​解, 记为$\psi_{k}$
$\sum_{k}\psi_{k}$是$G$的单位分解
$\varphi(x):=\sum_{k}\psi_{k}(x)\varphi(x)$. 每个点只属于有限个支撑集.

#### d答案
（4）记 $B_0:=\varnothing$ ，对 $j \in \mathbb{N}$ ，记 $B_j:=\left\{x \in G:|x| \leqslant j, d(x, \partial G) \geqslant \frac{1}{j}\right\}, G_j= B_j \backslash \stackrel{\circ}{B}_{j-1}$ ．显然每个 $G_j$ 都是紧集且 $G=\bigcup_{j=1}^{\infty} G_j$ ．定义开集族 $\mathcal{V}_1=\mathcal{V}_2=\{U_\gamma \cap \stackrel{\circ}{B}_3：\gamma \in \Gamma\}, \mathcal{V}_j=\left\{U_\gamma \cap \stackrel{\circ}{B}_{j+1} \cap B_{j-2}^c: \gamma \in \Gamma\right\}, j \geqslant 3 . \forall j \in \mathbb{N}$ ，易见 $\mathcal{V}_j$ 是紧集 $G_j$ 的开覆盖，于是由有限覆盖定理和《讲义》9．2．6 小节命题 6 可知，存在 $G_j$ 的从属于 $\mathcal{V}_j$ 的有限 $C^{(\infty)}$ 类单位分解 $E_j$ ．

- 根据单位分解定理，对于紧集 $G_j$ 和其开覆盖 $\mathcal{V}_j$ ，存在一个有限的光滑函数族 $E_j$ ，满足：
- 每个函数 $e \in E_j$ 是光滑的，且 $\operatorname{supp}(e)$ 包含在 $\mathcal{V}_j$ 的某个成员（从而包含在某个 $U_\gamma$ ）中。
- 在 $G_j$ 上，有 $\sum_{e \in E_j} e(x)=1$ ，且在 $G_j$ 附近， $0 \leq e(x) \leq 1$ 。

进而 $\forall x \in G$ ，易见 $\lambda(x)=\sum_{j=1}^{\infty} \sum_{e \in E_j} e(x)$ 只包含有限个非零项且 $\lambda(x)>0$ ．因此将可数函数族 $\left\{\frac{e(x)}{\lambda(x)} \varphi(x): e \in \bigcup_{j=1}^{\infty} E_j\right\}$ 排成函数序列 $\left\{\varphi_k, k \in \mathbb{N}\right\}$ 即满足要求．

> [!Note] 题目6-e
> e）请证明：广义函数意义下的可积函数集合 $C_0^{(\infty)}(G)$ 在相应正则广义函数集合 $C^{(\infty)}(G)$中处处稠密．

根据6-d, 和序列$S_{N}(x)=\sum_{k=1}^{N}\varphi_{k}(x)$ 满足$\lim_{N\to\infty}S_{N}(x)=\varphi(x)$
由于$\varphi$是$C^{\infty}(G)$中任意的元素, 稠密性成立.

> [!Note] 题目6-f
> f）设 $F_1, F_2$ 是 $\mathcal{D}^{\prime}(G)$ 中的两个广义函数，$U \subset G$ 是开集．如果等式 $\left\langle F_1, \varphi\right\rangle=\left\langle F_2, \varphi\right\rangle$ 对于在 $U$ 中有支撑集的任何 $\varphi \in \mathcal{D}(G)$ 都成立，就认为这两个广义函数在开集 $U$ 上相等。如果 $F_1, F_2$ 在点 $x \in G$ 的某个开邻域 $U(x) \subset G$ 上相等，就认为这两个广义函数在点 $x$ 局部相等．请证明：$\left(F_1=F_2\right) \Leftrightarrow\left(F_1=F_2\right.$ 在任何点 $x \in G$ 局部成立）．

根据局部成立性, 可以找到开覆盖$U\gamma$​覆盖$G$
利用6-d, 对于任意G的开覆盖, 都有$\sum_{k}\varphi_{k}=\varphi$, 且$supp\varphi_{k}\subset U_{\gamma}$ 
从而$\langle F_{1},\varphi \rangle=\lim_{k\to\infty}\langle F_{1},\varphi_{k} \rangle=\lim_{k\to\infty}\langle F_{2},\varphi_{k} \rangle=\langle F_{2},\varphi \rangle$
# 题目7

> [!Note] 题目7-a
> a）设
> $$
> \varphi(x):= \begin{cases}\exp \left(\frac{1}{|x|^2-1}\right), & |x|<1 \\ 0, & |x| \geqslant 1\end{cases}
> $$
> 请证明：对于 $\mathbb{R}$ 上的任何局部可积函数 $f$ ，当 $\varepsilon \rightarrow+0$ 时， $\int_{\mathbb{R}} f(x) \varphi_{\varepsilon}(x) d x \rightarrow 0$ ，其中 $\varphi_{\varepsilon}(x)= \varphi(x / \varepsilon)$ ．

令$t= \frac{x}{\varepsilon}$
$$
\int_{\mathbb{R}}f(x)\varphi_{\varepsilon}(x)dx=\varepsilon \int_{-\frac{1}{\varepsilon}}^{\frac{1}{\varepsilon}} f(t\varepsilon) \varphi(t)\, dt
$$
$$
\lim_{\varepsilon\to0+}\left\lvert \varepsilon \int_{-\frac{1}{\varepsilon}}^{\frac{1}{\varepsilon}} f(t\varepsilon) \varphi(t) \, dx    \right\rvert  \leq\frac{\varepsilon}{e}\lim_{\varepsilon\to0+} \left\lvert  \int_{-1}^{1} f(t\varepsilon) \, dt   \right\rvert = \frac{1}{e}\lim_{\varepsilon\to0+} |\int_{-\varepsilon}^{\varepsilon} f(t) \, dt| =0
$$


> [!Note] 题目7-b
> b）请利用以上结果和 $\left\langle\delta, \varphi_{\varepsilon}\right\rangle=\varphi(0) \neq 0$ 证明：广义函数 $\delta$ 不是正则的．

$\langle \delta,\varphi_{\varepsilon} \rangle\neq0$, 则$\varepsilon \to0$时积分也不是0. 从而得到$\delta$一定不能用局部可积函数表示

> [!Note] 题目7-c
> c）请证明：在 $\mathcal{D}^{\prime}$ 中收敛到广义函数 $\delta$ 的正则广义函数序列（甚至 $C_0^{(\infty)}$ 类函数序列）存在。 （其实，任何广义函数都是与 $\mathcal{D}=C_0^{(\infty)}$ 中的函数相对应的正则广义函数的极限。在这个意义下，正则广义函数组成在 $\mathcal{D}^{\prime}$ 中处处稠密的集合，这类似于有理数集 $\mathbb{Q}$ 在实数集 $\mathbb{R}$ 中处处稠密．）

$\varphi_{\alpha}=\frac{1}{\alpha}\varphi\left( \frac{x}{\alpha} \right)$ 是一个$\delta$ 函数族.
$$
\langle \varphi_{\alpha},f \rangle =\int_{-1}^{1} \varphi(t)f(\alpha t) \, dt
$$
$$
\left\lvert  \int_{-1}^{1} \varphi(t)(f(\alpha t)-f(0)) \, dt   \right\rvert \leq\varepsilon \left\lvert  \int_{-1}^{1} \varphi(t) \, dt   \right\rvert =\varepsilon
$$
所以$\lim_{\alpha\to0+}\langle \varphi_{\alpha},f \rangle=\langle \delta,f \rangle$

# 题目8

> [!Note] 题目8-a
> a）请计算下列广义函数 $F \in \mathcal{D}^{\prime}$ 在函数 $\varphi \in \mathcal{D}$ 上的值 $\langle F, \varphi\rangle$ ： $F=\sin x \delta ; F=2 \cos x \delta ; F=\left(1+x^2\right) \delta$ ．

1. $\int_{G}\sin x\varphi(0)dx$
2. $\int_{G}2\cos x\varphi(0)dx$
3. $\int_{G}(1+x^{2})\varphi(0)$

#### 正确答案
$\langle \sin x\delta,\varphi \rangle=\langle \delta,\sin x\varphi \rangle=\sin0\varphi(0)=0$

> [!Note] 题目8-b
> b）请验证：乘以函数 $\psi \in C^{(\infty)}$ 的运算 $F \mapsto \psi F$ 是 $\mathcal{D}^{\prime}$ 中的连续运算．

假设$F_{n}\to F$, 也即对于任意$\varphi\in D$, $\langle F_{n},\varphi \rangle\to \langle F,\varphi \rangle$. $\langle F_{n}\psi,\varphi \rangle=\langle F_{n},\psi \cdot \varphi \rangle\to \langle F,\psi \cdot \varphi \rangle=\langle F\psi,\varphi \rangle$. 所以$\psi F_{n}\to \psi F$当$n\to \infty$

广义函数的弱收敛性. 对任意$\varphi,\langle F_{n},\varphi \rangle\to \langle F,\varphi \rangle$

> [!Note] 题目8-c
> c）请验证：广义函数的线性运算在 $\mathcal{D}^{\prime}$ 中是连续的．

$\langle F+G,\varphi \rangle=\langle F,\varphi \rangle+\langle G,\varphi \rangle$​	
如果$F_{n}\to F,G_{n}\to G$, 则显然有$F_{n}+G_{n}\to F+G$作为分布极限存在. 再加上b, 可知广义函数的线性运算在$\mathcal{D}'$中是连续的.

$$
\left\langle\alpha F_{1, n}+\beta F_{2, n}, \varphi\right\rangle=\alpha\left\langle F_{1, n}, \varphi\right\rangle+\beta\left\langle F_{2, n}, \varphi\right\rangle \rightarrow \alpha\left\langle F_1, \varphi\right\rangle+\beta\left\langle F_2, \varphi\right\rangle=\left\langle\alpha F_1+\beta F_2, \varphi\right\rangle,
$$
# 题目9

> [!Note] 题目9-a
> a）请证明：如果 $F$ 是由函数
> $$
> f(x)= \begin{cases}0, & x \leqslant 0 \\ x, & x>0\end{cases}
> $$
> 产生的正则分布，则 $F^{\prime}=H$ ，其中 $H$ 是赫维赛德函数所对应的分布．

$$
\langle F',\varphi \rangle =-\int_{\mathbb{R}}f(x)\varphi'(x)dx=-\int_{0}^{+\infty} x\varphi'(x) \, dx =-x\varphi(x)|_{o}^{+\infty}+\int_{0}^{+\infty} \varphi(x) \, dx =\langle H,\varphi \rangle
$$

> [!Note] 题目9-b
> b）请计算函数 $|x|$ 所对应的分布的导数．

$$
\langle \lvert x \rvert ,\varphi \rangle =-\int_{-\infty}^{0} \varphi(x) \, dx +\int_{0}^{+\infty} \varphi(x) \, dx
$$
令
$$G=
\begin{cases}
-1 & x\leq0 \\
1 & x>0
\end{cases}
$$
$G=\lvert x \rvert'$ 