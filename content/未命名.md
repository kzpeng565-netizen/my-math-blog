# 8. 最大值最小值

**先从图像上理解最大值:** 

<div class="blank-xl"></div>

最大值(是一个函数值): 存在$x_{0}\in D$, 使得对任意的$x\in D$, 有$f(x_{0})\geq f(x)$ , 则$f(x_{0})$称为最大值, $x_{0}$称为最大值点
最小值: 存在$x_{0}\in D$, 使得对任意的$x\in D$, 有$f(x_{0})\leq f(x)$ , 则$f(x_{0})$称为最小值, $x_{0}$称为最小值点

<div class="blank"></div>

**最大值和最小值未必存在, 而且与定义域的选取有关系**

<div class="blank"></div>




**例1　最值与定义域有关**

分别判断函数  
$$
f(x)=x^2
$$
在下列定义域上是否存在最大值和最小值：

1. $x\in[-2,3]$；
2. $x\in(-2,3)$；
3. $x\in(-2,3]$。

<div class="blank-l"></div>


**例2　绝对值函数的最值**

已知  
$$
f(x)=|x-1|+2,\qquad x\in[-2,4].
$$

1. 画出函数在给定定义域内的大致图像；
2. 求函数的最大值和最小值；
3. 写出最大值点和最小值点。

<div class="blank-l"></div>

**例3 判断最值是否存在**

已知  
$$
f(x)=\frac{2}{x-1},\qquad x\in[2,6).
$$

根据函数图像判断：
![[Pasted image 20260802103247.png|300]]
1. 函数是否存在最大值？若存在，求出最大值及最大值点；
2. 函数是否存在最小值？说明理由。

<div class="blank-l"></div>


# 9. 轴对称函数与偶函数

$$
f(x)=(x-1)^2+1
$$

对称轴是什么？怎么在图像上反映？

<div class="blank-xl"></div>

第一种表示方法: 
$$
f(1-x)=f(1+x)
$$

<div class="blank-xl"></div>

第二种表示方法:

$$
f(x)=f(2-x)
$$

<div class="blank-xl"></div>

第三种表示方法:
$$
\forall x_{1},x_{2}\quad x_{1}+x_{2}=1 \implies f(x_{1})=f(x_{2})
$$

<div class="blank-xl"></div>

**一般的轴对称函数表达式:**

<div class="blank-xl"></div>

偶函数就是轴对称$x=0$的函数

# 10. 中心对称与奇函数

中心对称的例子: 平行四边形
**奇函数的定义:** 
$$
f(x)+f(-x)=0
$$

<div class="blank-xl"></div>

奇函数的例子:
$$
f(x)=x
$$
$$
f(x)=x^{3}
$$
$$
f(x)=\frac{1}{x}
$$

**奇函数的推广: 一般的中心对称函数**
对称点为(a,b)
$$
\begin{cases}
\frac{x_{1}+x_{2}}{2}=a \\
\frac{f(x_{1})+f(x_{2})}{2}=b
\end{cases}
$$

<div class="blank-xl"></div>

或者
$$
f(x)+f(2a-x)=2b
$$

