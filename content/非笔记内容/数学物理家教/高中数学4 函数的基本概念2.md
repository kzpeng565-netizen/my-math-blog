# 函数的概念回顾

**如何理解$f(x)$这个符号**

$x$ ：输入的数
$f$ ：一种确定的对应规则
$f(x)$ ：把 $x$ 代入规则 $f$ 后得到的输出

例如 $f(x)=2 x+1$ ，实际上是在规定：
规则 $f$ ：输入一个数，先乘以 2 ，再加1。
因此：
$$
f(3)=2 \times 3+1=7
$$

最好用不同字母强调＂规则＂和＂结果＂不同：

$$
f(x)=2 x+1, \quad g(x)=x^2
$$


<div class="blank-l"></div>

**如何理解任意的x都有唯一的y对应**

**一个输入只能有一个最终答案**

（3）

$x\in A,\ y\in B$。

![[Pasted image 20260801101637.png|400]]

定义域：____________________

值域：____________________


(4)
下列图像中，可能是函数图像的有：____________________。（无需写出定义域和值域）

![[Pasted image 20260801101704.png]]

<div class="blank-l"></div>

2．求下列函数的定义域。
（1）$f(x)=\sqrt{x+3}+\frac{1}{x+2}$ ；
（2）$f(x)=\sqrt{1-x}+\sqrt{x+3}-1$ ；
（3）$f(x)=\frac{1}{\sqrt{x^2-1}}$ ．

<div class="blank-xl"></div>

# 4. 绝对值函数的图像

1．下列函数中，与 $y=x$ 是同一个函数的是：
（1）$y=\sqrt{x^2}$ ；
（2）$u=\sqrt[3]{v^3}$ ；（自变量为 $v$ ）
（3）$y=(\sqrt{x})^2$ ；
（4）$y=\frac{x^2}{x}$ ．

<div class="blank-xl"></div>

​	2．设 $f(x)=3 x^2+2 x$ ．
求 $f(2), f(-2), f(2)+f(-2)$ ；
求 $f(a), f(-a), f(a)+f(-a)$ ；
求 $f(x-1), f(x+1), f(x-1)+f(x$
求 $f(\sqrt{x})$ ；

<div class="blank-xl"></div>

3．画出下列函数的图像。
(1) $f(x)=|x-2|$ ；
(2) $f(x)=|x-1|+|x-2|$ ；
(3) $f(x)=[x]$ ，其中 $[x]$ 表示不超过 $x$ 的最大整数
(4) $f(x)= \begin{cases}-x, & -1<x<0, \\ 2 x, & 0 \leq x<1, \\ x, & 1 \leq x \leq 2 .\end{cases}$

<div class="blank-xl"></div>

<div class="blank-l"></div>


# 5. 函数的定义域和值域练习

1．设 $f(x)=-x+1, g(x)=(x-1)^2$ ．
（1）画出 $f(x), g(x)$ 的图像；
（2）对任意 $x \in \mathbb{R}$ ，用 $m(x)$ 表示 $f(x), g(x)$ 中的较小者，记为 $m(x)=$ $\min \{f(x), g(x)\}$ ．分别用图像法和解析法衣示 $m(x)$ ．
（3）判断点 $A(1,0), B(2,1), C(3,4)$ 在函数图像上吗？

<div class="blank-xl"></div>


2．完成下列各题。
（1）已知 $f(x)$ 的定义域为 $[0,1]$ ，则 $f(x+1)$ 的定义域为 $\_\_\_\_$；
（2）已知 $f(x+1)$ 的定义域为 $[0,1]$ ，则 $f(x)$ 的定义域为 $\_\_\_\_$；
（3）已知 $f(2 x-1)$ 的定义域为 $[-1,3]$ ，则 $f(x)$ 的定义域为 $\_\_\_\_$；
（4）已知 $f(x)$ 的定义域为 $\mathbb{R}$ ，值域为 $[0,1]$ ，则 $f(x+1)$ 的定义域为 $\_\_\_\_$，值域为 $\_\_\_\_$；
（5）已知 $f(x+1)$ 的定义域为 $\mathbb{R}$ ，值域为 $[0,1]$ ，则 $f(x)$ 的定义域为 $\_\_\_\_$，值域为 $\_\_\_\_$；
（6）已知 $f(x)$ 的定义域为 $[0,1]$ ，值域为 $[1,2]$ ，则 $f(x-1)+1$ 的定义域为
$\_\_\_\_$，值域为 $\_\_\_\_$。

**谁是自变量: 默认x是自变量**

<div class="blank-xl"></div>


3. 某市招手即停公共汽车的票价按下列规则制定：
（1） 5 km 以内（含），票价 2 元；
（2）5 km 以上，每增加 5 km，票价增加 1 元（不足 5 km 按 5 km 计算）。
若某线路总里程为 20 km，请写出票价与里程之间的函数解析式，并画出函数图像。

<div class="blank-xl"></div>


4．探究是否存在不同的函数 $f(x), g(x)$ ，满足：
（1）定义域相同，值域相同，但对应关系不同；
（2）定义域相同，对应关系相同，但值域不同。

<div class="blank-xl"></div>

# 6. 单调性

例子: $y=x^{2}$, 在$x<0$时, $y$随$x$的增大而减小. 在$x>0$时, $y$随$x$的增大而增大.

<div class="blank-xl"></div>

单调性的严格定义: 
随$x$的增大而增大
$$
x_{1},x_{2}\in D\quad x_{1}<x_{2}\text{时},\ \text{有}f(x_{1})<f(x_{2})
$$
随$x$的增大而减小
$$
x_{1},x_{2}\in D\quad x_{1}<x_{2}\text{时},\ \text{有}f(x_{1})>f(x_{2})
$$

1. 讨论二次函数的单调区间
2. 一次函数的单调性

<div class="blank-xl"></div>

# 7. 最大值最小值

**先从图像上理解最大值:** 

<div class="blank-xl"></div>

最大值(是一个函数值): 存在$x_{0}\in D$, 使得对任意的$x\in D$, 有$f(x_{0})\geq f(x)$ , 则$f(x_{0})$称为最大值, $x_{0}$称为最大值点
最小值: 存在$x_{0}\in D$, 使得对任意的$x\in D$, 有$f(x_{0})\leq f(x)$ , 则$f(x_{0})$称为最小值, $x_{0}$称为最小值点

<div class="blank"></div>

**最大值和最小值未必存在, 而且与定义域的选取有关系**

<div class="blank"></div>




**例1　最值与定义域有关**

分别判断函数  
$$
f(x)=x^2
$$
在下列定义域上是否存在最大值和最小值：

1. $x\in[-2,3]$；
2. $x\in(-2,3)$；
3. $x\in(-2,3]$。


**例2　绝对值函数的最值**

已知  
$$
f(x)=|x-1|+2,\qquad x\in[-2,4].
$$

1. 画出函数在给定定义域内的大致图像；
2. 求函数的最大值和最小值；
3. 写出最大值点和最小值点。

**例3 判断最值是否存在**

已知  
$$
f(x)=\frac{2}{x-1},\qquad x\in[2,6).
$$

根据函数图像判断：
![[Pasted image 20260802103247.png|300]]
1. 函数是否存在最大值？若存在，求出最大值及最大值点；
2. 函数是否存在最小值？说明理由。