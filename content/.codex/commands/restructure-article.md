# 整理文章结构

使用 `$math-article-structure`。

任务：

改进数学博客文章的结构。

第一轮必须只读分析，不直接修改。

固定输出格式：

```text
当前主线：
读者可能卡住的地方：
建议大纲：
移动映射：
需要保持不变的小节：
需要局部重写的小节：
评分：
修改前是否需要确认：是
```

用户批准后，使用 `structured_edit`。除非用户明确要求全文重写/重构，否则保留原有数学内容。
